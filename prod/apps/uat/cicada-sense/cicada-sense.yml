---
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: cicada-sense-uat
  namespace: argocd
  annotations:
    argocd.argoproj.io/application-repository: cicada-sense
    argocd.argoproj.io/environment: "uat"
    argocd.argoproj.io/sync-wave: "1"
    argocd.argoproj.io/deployment-id: "5328409277"
  labels:
    layer: applications
    service: cicada-sense
    component: main
    environment: uat
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  destination:
    namespace: cicada-sense-uat
    server: https://192.168.200.102:443
  syncPolicy:
    syncOptions:
      - CreateNamespace=false
      - ServerSideApply=true
      - SkipDryRunOnMissingResource=true
    automated:
      prune: true
      selfHeal: true
  sources:
    - repoURL: ghcr.io/annoying-anemone/cicada-sense/charts/application
      targetRevision: "0.1.0-rc.0" # Will be updated by deploy workflow
      chart: cicada-sense
      helm:
        values: |
          backend:
            ingress:
              enabled: true
              className: "traefik"
              hosts:
                - host: cicada-sense-uat.user-00.hoverkraft.cloud
                  paths:
                    - path: /api
                      pathType: Prefix
                    - path: /socket.io
                      pathType: Prefix
          frontend:
            ingress:
              enabled: true
              className: "traefik"
              hosts:
                - host: cicada-sense-uat.user-00.hoverkraft.cloud
                  paths:
                    - path: /
                      pathType: ImplementationSpecific
          live-data-generator:
            api:
              ingress:
                enabled: true
                className: "traefik"
                hosts:
                  - host: cicada-sense-generator-uat.user-00.hoverkraft.cloud
                    paths:
                      - path: /api
                        pathType: Prefix
            ui:
              ingress:
                enabled: true
                className: "traefik"
                hosts:
                  - host: cicada-sense-generator-uat.user-00.hoverkraft.cloud
                    paths:
                      - path: /
                        pathType: Prefix
          deploymentId: "5328409277"
